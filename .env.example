# ========================================
# ENVIRONMENT CONFIGURATION
# ========================================
# Environment: "development", "staging", or "production"
NODE_ENV="development"
NUXT_PUBLIC_ENVIRONMENT="development"

# ========================================
# SESSION & SECURITY (REQUIRED)
# ========================================
# Session password for encrypting session cookies
# Must be at least 32 characters long
# Generate with: openssl rand -base64 64
NUXT_SESSION_PASSWORD="your-64-character-random-string-here-change-in-production"

# JWT secret for signing email confirmation & password reset tokens
# Should be at least 64 characters long
# Generate with: openssl rand -base64 64
NUXT_JWT_SECRET="your-64-character-random-jwt-secret-here-change-in-production"

# ========================================
# OAUTH PROVIDERS (OPTIONAL)
# ========================================
# Configure OAuth providers you want to enable
# Get credentials from respective provider dashboards
# Supported providers: GitHub, Google, Discord, Microsoft, Facebook, GitLab,
# Linear, Slack, Spotify, Twitch, and 30+ more
# See: https://github.com/atinux/nuxt-auth-utils for full list

# GitHub OAuth
# Get credentials from: https://github.com/settings/developers
NUXT_OAUTH_GITHUB_CLIENT_ID=""
NUXT_OAUTH_GITHUB_CLIENT_SECRET=""
# NUXT_OAUTH_GITHUB_REDIRECT_URL=""  # Optional: override default redirect URL

# Google OAuth
# Get credentials from: https://console.cloud.google.com/apis/credentials
NUXT_OAUTH_GOOGLE_CLIENT_ID=""
NUXT_OAUTH_GOOGLE_CLIENT_SECRET=""
# NUXT_OAUTH_GOOGLE_REDIRECT_URL=""  # Optional: override default redirect URL

# Discord OAuth
# Get credentials from: https://discord.com/developers/applications
NUXT_OAUTH_DISCORD_CLIENT_ID=""
NUXT_OAUTH_DISCORD_CLIENT_SECRET=""
# NUXT_OAUTH_DISCORD_REDIRECT_URL=""  # Optional: override default redirect URL

# Microsoft OAuth
# Get credentials from: https://portal.azure.com/#view/Microsoft_AAD_RegisteredApps/ApplicationsListBlade
NUXT_OAUTH_MICROSOFT_CLIENT_ID=""
NUXT_OAUTH_MICROSOFT_CLIENT_SECRET=""
# NUXT_OAUTH_MICROSOFT_REDIRECT_URL=""  # Optional: override default redirect URL

# Add other OAuth providers as needed following the same pattern:
# NUXT_OAUTH_<PROVIDER>_CLIENT_ID=""
# NUXT_OAUTH_<PROVIDER>_CLIENT_SECRET=""
# Provider names must be in UPPERCASE (e.g., FACEBOOK, GITLAB, SLACK, etc.)

# ========================================
# EMAIL CONFIGURATION (OPTIONAL)
# ========================================
# Email provider for sending transactional emails
# Options: "none" (default, disables email), "resend", "postmark"
NUXT_EMAIL_PROVIDER="none"

# Email provider API key (required if provider is not "none")
# Resend: Get from https://resend.com/api-keys
# Postmark: Get from https://account.postmarkapp.com/servers
NUXT_EMAIL_API_KEY=""

# From email address for transactional emails
NUXT_EMAIL_FROM="noreply@localhost"

# ========================================
# CLOUDFLARE TURNSTILE (OPTIONAL)
# ========================================
# Bot protection using Cloudflare Turnstile
# Get keys from: https://dash.cloudflare.com/turnstile
# Leave empty to disable bot protection

# Public site key (visible to client)
NUXT_PUBLIC_TURNSTILE_SITE_KEY=""

# Secret key (server-side only)
NUXT_TURNSTILE_SECRET_KEY=""

# ========================================
# DATABASE SEEDING (OPTIONAL)
# ========================================
# Secret key for protecting production database seeding via API
# Only needed if you plan to seed production database remotely
# Generate with: openssl rand -base64 32
NUXT_SEED_SECRET=""

# ========================================
# MULTITENANCY (OPTIONAL)
# ========================================
# Enable multi-tenant mode (requires manual DB provisioning per tenant)
# Default: false (single-tenant mode with one database for all data)
# Uncomment to enable:
# NUXT_MULTITENANCY_ENABLED="false"
# NUXT_PUBLIC_MULTITENANCY_ENABLED="false"

# ========================================
# RBAC - ROLE-BASED ACCESS CONTROL (OPTIONAL)
# ========================================
# Enable or disable role-based access control enforcement
# Default: true (RBAC enabled)
# Set to "false" to disable RBAC:
# NUXT_RBAC_ENABLED="true"

# ========================================
# ANALYTICS (OPTIONAL)
# ========================================
# PostHog Analytics - Currently commented out in nuxt.config.ts
# Uncomment in nuxt.config.ts and set these to enable:
# NUXT_PUBLIC_POSTHOG_PUBLIC_KEY=""
# NUXT_PUBLIC_POSTHOG_HOST="https://us.i.posthog.com"
# NUXT_PUBLIC_POSTHOG_AUTOCAPTURE="true"

# ========================================
# CI/CD VARIABLES (AUTO-SET)
# ========================================
# These are automatically set by CI/CD environments
# You typically don't need to set these manually
# CI="false"
# GITHUB_ACTOR="username"
# USER="username"

# ========================================
# CLOUDFLARE WORKERS BINDINGS
# ========================================
# These are configured in wrangler.jsonc files, not in .env
# - DB: D1 Database binding (required)
# - KV: KV Namespace binding (optional)
# - R2: R2 Bucket binding (optional)
# See wrangler.staging.jsonc and wrangler.production.jsonc for configuration

# ========================================
# DEVELOPMENT NOTES
# ========================================
# 1. For local development, copy this file to .env and fill in your values
# 2. For Cloudflare Workers deployment, configure secrets in wrangler.jsonc
# 3. All NUXT_ prefixed variables override nuxt.config.ts runtimeConfig
# 4. NUXT_PUBLIC_ prefixed variables are exposed to the client
# 5. Never commit .env files to version control
# 6. Generate secure random strings with: openssl rand -base64 64
