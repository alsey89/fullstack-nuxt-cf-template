meta {
  name: Reset Password
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/api/v1/auth/password/reset
  body: json
  auth: none
}

body:json {
  {
    "token": "your-password-reset-token-here",
    "newPassword": "testtesttest"
  }
}

docs {
  # Reset Password
  - Reset password with the token sent via email
  - Token and newPassword should be sent in the request body
  - Note: API expects "newPassword" field, not "password"
}
