meta {
  name: Confirm Email
  type: http
  seq: 6
}

post {
  url: {{baseUrl}}/api/v1/auth/email/confirm
  body: json
  auth: none
}

body:json {
  {
    "token": "your-email-confirmation-token-here"
  }
}

docs {
  # Confirm Email
  - Confirm email address with the token sent via email
  - Token should be sent in the request body, not as Bearer token
}
